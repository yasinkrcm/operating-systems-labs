# Example 02 – File Read/Write

## Purpose
To read data from a file and write it to another file, demonstrating basic file operations.

## System Calls / Functions Used
- open
- read
- write
- printf

## How It Works
The program first reads the "text.txt" file, writes the read data to a file named "target", and prints the number of bytes read.

## How to Compile and Run
$ gcc main.c -o app
$ ./app
